#index.js
    const Sequelize = require('sequelize');
    const sequelize = new Sequelize("teste", "root", "root", {
        host: "localhost",
        dialect: 'mysql'
    });
    //Verifica se conectou no banco 
    //   then() //se deu certo 
         catch() //deu errado
    sequelize.authenticate()
    .then(function () {
        console.log('Conectado com sucesso')
    }).catch(function (erro) {
        console.log('Falha ao se conectar:' + erro);
    })
