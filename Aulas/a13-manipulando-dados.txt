#Inserindo os dados na tabela
    INSERT INTO usuarios(nome, email, idade) VALUES(
        "Bruno Cesar",
        "brunocesardeoliveira2018@gmail.com",
        40
    );
    INSERT INTO usuarios(nome, email, idade) VALUES(
        "Fulano ",
        "fulano@gmail.com",
        50
    );
    INSERT INTO usuarios(nome, email, idade) VALUES(
        "Beltranor",
        "beltrano@gmail.com",
        50
    );
    INSERT INTO usuarios(nome, email, idade) VALUES(
        "Aquele",
        "aqueleo@gmail.com",
        8
    );

#Selecionando todos os dados da tabela
    SELECT * FROM usuarios;

#Selecionando um usuário específico
    SELECT * FROM usuarios WHERE idade =8;
    SELECT * FROM usuarios WHERE nome = "Bruno Cesar";
    SELECT * FROM usuarios WHERE idade >=18;
